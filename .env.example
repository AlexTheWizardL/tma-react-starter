# ===========================================
# TMA React Starter - Environment Variables
# ===========================================
#
# Copy this file to set up your environment:
#   cp .env.example .env.development   # For local dev with ngrok
#   cp .env.example .env.production    # For production deployment
#
# ===========================================
# App Variables (embedded in build)
# ===========================================
# Must use VITE_ prefix to be exposed to code
# Access via: import.meta.env.VITE_*

# Example API endpoint
# VITE_API_URL=https://api.example.com

# ===========================================
# Bot Setup Variables (NOT embedded)
# ===========================================
# Used by scripts only - never exposed to client

# Your bot token from @BotFather
BOT_TOKEN=

# Your app URL
# .env.development: leave empty (ngrok sets it automatically)
# .env.production: https://yourusername.github.io/your-repo/
VITE_APP_URL=
